# Created by https://www.toptal.com/developers/gitignore/api/rust,direnv
# Edit at https://www.toptal.com/developers/gitignore?templates=rust,direnv

### Direnv ###
.direnv
.envrc
.env

### Pre-commit ###
.pre-commit-config.yaml

### Rust ###
# Generated by Cargo
# will have compiled files and executables
debug/
target/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

### Misc ###
data.db
data.db*

# End of https://www.toptal.com/developers/gitignore/api/rust,direnv

# Should use .envrc for (nix-)direnv! We export devShells already.
# This allows (nix-)direnv user to `direnv allow` and the devShell pulls down
!.envrc
# Nix build results
result
# The repo explicitly has Cargo.lock, but the above template includes ignores this
!Cargo.lock

